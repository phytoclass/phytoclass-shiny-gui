v1.0.0.1
* `do_matrix_checks` is now set to false.

v0.0.6+
* correct output for “number of clusters”.
* +clusterInspector: choose a cluster, table then coming up that shows the samples within that cluster. 
